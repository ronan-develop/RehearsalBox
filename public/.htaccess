RewriteEngine On

# Fichiers/dossiers réels (assets) servis directement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tout le reste vers le front controller
RewriteRule ^ index.php [L]
